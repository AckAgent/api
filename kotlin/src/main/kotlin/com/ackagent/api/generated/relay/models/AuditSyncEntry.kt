/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package com.ackagent.api.generated.relay.models


import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

/**
 * Single entry in the device audit chain for tamper-evident logging.
 *
 * @param entryId Unique identifier for this audit chain entry.
 * @param sequence Monotonically increasing sequence number within the device's chain.
 * @param timestamp ISO 8601 timestamp when the entry was created on the device.
 * @param entryHash SHA256 hash of the entry contents for integrity verification.
 * @param entryType Type of audit event (e.g., signing_request, key_generation).
 * @param encryptedPayloadHash Hash of the encrypted payload for verification without decryption.
 * @param plaintextHash Hash of the plaintext for verification by the device only.
 * @param deviceSignature Base64-encoded ECDSA P-256 signature over the entry using the device's audit signing key.
 * @param auditSigningPublicKeyHex Lowercase hex-encoded P-256 ECDSA public key of the purpose-scoped audit signing key (66 hex chars, 33 bytes: 0x02/0x03 || X (compressed P-256)).
 * @param prevHash Hash of the previous entry in the chain, creating a linked list.
 * @param requestHash Hash of the associated signing request, if applicable.
 * @param challengeHash Hash of the challenge used for signing, if applicable.
 * @param challengeContext Additional context for the challenge (e.g., domain, application).
 */
@Serializable

data class AuditSyncEntry (

    /* Unique identifier for this audit chain entry. */
    @SerialName(value = "entryId")
    val entryId: kotlin.String,

    /* Monotonically increasing sequence number within the device's chain. */
    @SerialName(value = "sequence")
    val sequence: kotlin.Long,

    /* ISO 8601 timestamp when the entry was created on the device. */
    @SerialName(value = "timestamp")
    val timestamp: kotlin.String,

    /* SHA256 hash of the entry contents for integrity verification. */
    @SerialName(value = "entryHash")
    val entryHash: kotlin.String,

    /* Type of audit event (e.g., signing_request, key_generation). */
    @SerialName(value = "entryType")
    val entryType: kotlin.String,

    /* Hash of the encrypted payload for verification without decryption. */
    @SerialName(value = "encryptedPayloadHash")
    val encryptedPayloadHash: kotlin.String,

    /* Hash of the plaintext for verification by the device only. */
    @SerialName(value = "plaintextHash")
    val plaintextHash: kotlin.String,

    /* Base64-encoded ECDSA P-256 signature over the entry using the device's audit signing key. */
    @SerialName(value = "deviceSignature")
    val deviceSignature: kotlin.String,

    /* Lowercase hex-encoded P-256 ECDSA public key of the purpose-scoped audit signing key (66 hex chars, 33 bytes: 0x02/0x03 || X (compressed P-256)). */
    @SerialName(value = "auditSigningPublicKeyHex")
    val auditSigningPublicKeyHex: kotlin.String,

    /* Hash of the previous entry in the chain, creating a linked list. */
    @SerialName(value = "prevHash")
    val prevHash: kotlin.String? = null,

    /* Hash of the associated signing request, if applicable. */
    @SerialName(value = "requestHash")
    val requestHash: kotlin.String? = null,

    /* Hash of the challenge used for signing, if applicable. */
    @SerialName(value = "challengeHash")
    val challengeHash: kotlin.String? = null,

    /* Additional context for the challenge (e.g., domain, application). */
    @SerialName(value = "challengeContext")
    val challengeContext: kotlin.String? = null

) {


}

